{
  "Basic_Assert": {
    "prefix": "ASSERT",
    "body": [
      "function assert(msg, expected, actual) {",
      "  if (expected === actual) {",
      "    console.log('PASSED!');",
      "  } else {",
      "    console.log(msg + ': expected[' + expected + '] but got [' + actual + ']');",
      "  }",
      "}"
    ]
  }
}
